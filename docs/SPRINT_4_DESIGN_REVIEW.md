# Sprint 4 Design Review — Website Generation (the Design Stage)

**Status: design only. Nothing in this document has been implemented, and this review does not authorize implementation to begin.** Per standing process (`CLAUDE.md`'s Workflow section, `docs/SPRINT_3_DESIGN_REVIEW.md`'s own precedent), this exists to be reviewed and explicitly approved by the founder before any Sprint 4 code is written. It follows Sprint 3's close (`docs/SPRINT_3_REVIEW.md`, CTO score 9.0/10, Ship, tagged `v0.4.0-alpha`) and covers exactly what the founder directed: architecture, workflow, Mission Engine integration, the design pipeline, AI responsibilities, acceptance criteria, risks, and open questions for the next pipeline stage — website generation.

**Scope, per `docs/ARCHITECTURE_DECISIONS.md` ADR-010's named pipeline** (Discovery → Qualification → Research → Design → Proposal → Email → CRM → Learning → Analytics): this is the **Design** stage. A redesigned website is one artifact the Design stage produces, not the stage's whole identity — `docs/VISION_GUARDRAILS.md` is explicit that Obsidian OS is not a website builder and not a template marketplace, and every design principle below is written to hold that line, not quietly cross it because a website generator is genuinely what gets built first.

---

## 0. What this document is answering, and what it deliberately isn't

The founder asked for research into modern premium AI-assisted design workflows, an evaluation of what aligns with this codebase's existing philosophy, and a design document reacting to (not committing to) a specific conceptual pipeline. This document does that. It does **not** decide the category-weighting question `docs/SPRINT_3_REVIEW.md` flagged as an open founder decision (Opportunity Score weighting) — that's Analysis Engine debt, unrelated to this stage, and is called out again in Open Questions below only because Sprint 4's Design stage will be one of the first real consumers of that score and should not silently start depending on a number nobody has actually decided is trustworthy yet.

---

## 1. Research: what "premium AI-assisted design" actually means right now, and what to take from it

Web research (2026) on AI-assisted design workflows surfaced a consistent shape across several independent sources, summarized honestly rather than treated as settled fact:

- **Generic output is a workflow problem, not a model problem.** The recurring diagnosis: one prompt doing "taste, exploration, and code all at once" returns the statistical average — which is exactly what "looks like AI" means in practice. The fix isn't a better model, it's separating *what should this look like* (a creative/direction decision) from *build it* (an implementation decision) into distinct passes with distinct review points.
- **Plan the creative in text before generating anything visual.** Multiple sources converge on writing an explicit design brief/spec first — informed by real inputs (brand, audience, content), reviewed as text — before any component or layout gets generated. Text is cheap to iterate on and cheap for a human to review; a half-built page is neither.
- **Multi-pass refinement, graded per dimension, not "generate once and ship."** The pattern with the most direct precedent in this codebase: render, compare against explicit criteria across several independent dimensions (layout, typography, spacing, color, motion), grade each dimension, fix what's graded poorly, re-render, repeat up to a capped number of iterations. This is structurally the same idea as Sprint 3's Analysis Engine pipeline (raw → normalized → insight → score → report, each stage owned separately) and as `opportunity-report-service.test.ts`'s banned-terms check (grade the output against an explicit, checkable rule, not a vibe) — a pattern this codebase has already validated works, applied to a new domain.
- **Structured design constraints (tokens, a written style system) make AI output better, not just more consistent.** Giving a generation pass explicit typography scale, spacing rules, and color tokens up front — rather than free-associating on every request — is described repeatedly as what separates AI output that reads as considered from output that reads as generic. This maps directly onto how `docs/09-UI-Design-System.md` already governs Obsidian's *own* product surfaces.
- **Curated, industry-tagged reference libraries exist as a real category** (industry-sorted landing-page galleries, AI-searchable design-reference tools that extract color/typography/component patterns from real sites). These are explicitly positioned as *taste and direction* inputs, not as templates to fill in.
- **Separate exploration tools for unconventional layouts** (image-generation models proposing tilted/glass/3D layouts a coding agent tends to avoid because they're harder to build correctly) came up as a way to escape "coding-agent-shaped" default layouts.

**What's adopted, adapted, and rejected below is this review's judgment, not a repetition of the research** — the research describes what the current external practice looks like; the decisions below are about what actually fits Obsidian OS specifically.

### Adopted (fits directly)

**Separating direction from execution into distinct, independently-gradable passes** is adopted as the core structural idea for §4's pipeline below — it's the same lesson Sprint 3 already learned and encoded as ADR-011 (four single-responsibility services beat one large function), applied to design instead of analysis. **Writing the creative direction as a text brief before generating anything** is adopted for the same reason `insight-service.ts` exists as a distinct layer from `opportunity-report-service.ts`: a text artifact is reviewable, an assembled page is not, until it's too late to cheaply redirect it. **Explicit, checkable per-dimension QA grading, capped and iterative** is adopted directly — it's evidence-first architecture (ADR-013) applied to visual output instead of textual claims, and this codebase already has a working precedent for "grade against an explicit rule, fail the check if it's violated" in the banned-terms test.

### Adapted (right idea, needs a guardrail this codebase specifically requires)

**Curated, industry-tagged reference libraries** are adapted, not adopted outright: they inform *direction* (a mood, a layout family, a typography pairing appropriate to the target business's industry) and never supply content, copy, or a structure that gets filled in with the client's name and shipped. `docs/VISION_GUARDRAILS.md` is unambiguous — "every design a mission produces is bespoke work product for one specific prospect business... not a template pulled off a shelf and lightly customized" — and a reference library used carelessly is exactly the mechanism that guardrail exists to prevent. §5 below treats this as a hard line, not a style preference. **Structured design tokens/constraints** are adapted with one clarification that matters enough to state plainly: **`docs/09-UI-Design-System.md`'s specific tokens (the navy/near-black palette, Inter, dark-mode-only) govern Obsidian's own product surfaces — Mission Control, login, the Opportunity Report — and do not extend to what a generated client website looks like.** A generated site for a bakery and a generated site for a law firm should not both come out looking like Obsidian OS's own dashboard; they should look like premium sites for a bakery and a law firm, respectively. What *does* carry over universally, and is non-negotiable, is the underlying discipline — real typographic scale, generous and deliberate spacing, restrained motion, no visual noise — not the specific hex values.

### Rejected, or held as an open question rather than a default

**A separate image-generation exploration step for unconventional (tilted/glass/3D) layouts** is not adopted as a default. Obsidian's own design language is explicit about restraint over cleverness ("no loud gradients, no cartoon graphics... restraint and confidence, not minimalism for its own sake" — `docs/09-UI-Design-System.md`), and "premium" in this codebase's established vocabulary has always meant quiet and confident, not maximalist. There will be real businesses (a creative agency, a nightlife venue) for which a bolder layout is the *correct*, evidence-grounded choice — but that should be a direction decision made deliberately per-mission from real business-analysis input, not a default generation mode reached for because it produces more visually striking demos. Flagged as Open Question 3. **Adopting a specific third-party paid design-reference platform as a hard dependency** is rejected for the same reason ADR-006 rejected adopting a real message queue before Sprint 2 had a consumer for it: premature infrastructure commitment with real recurring cost, ahead of confirming the smaller, in-house-curated alternative isn't sufficient. Flagged as Open Question 4.

---

## 2. Architecture

**New subsystem: the Design Engine**, following Sprint 3's now-proven pattern (ADR-011) of one job per service rather than one large generation function:

- **`lib/services/design-brief-service.ts`** (proposed) — reads a mission's already-completed Analysis Engine output (`website_analyses`, the assembled `OpportunityReport`, its Insights) plus `companies` data, and produces a **Design Brief**: a structured, text-first artifact naming the target audience, the business's actual stated or inferred positioning, which specific Insights the new design must address, and a proposed direction (layout family, typographic mood, color direction) with its reasoning. This is the "plan the creative in text first" step from §1, and it is the layer where reference-library input (§5) enters — as citations informing the brief's reasoning, never as a structure the brief hands downstream.
- **`lib/services/design-generation-service.ts`** (proposed) — takes the Design Brief and produces the actual site: layout/wireframe, component assembly, typography application, spacing application, and (last, most restrained) motion. Whether this is one service with internal passes or several smaller services mirroring §4's stages exactly is an implementation decision for the next phase, not this review — but per ADR-011's lesson, each pass should be a distinct, independently-testable step, not one prompt doing everything at once.
- **`lib/services/design-qa-service.ts`** (proposed) — renders the generated output and grades it against explicit, named dimensions (layout, typography, spacing, motion, mobile, evidence-grounding of copy), the same "PASS/MINOR/MODERATE/CRITICAL"-shaped grading the research surfaced, capped at a small fixed number of iterations rather than looping until subjectively "good enough." This is where Sprint 3's evidence-first standard gets a visual-domain equivalent: a design doesn't get called premium because it looks confident, it gets called premium because it passed named, checkable criteria.

Per Architecture Principle 1, none of these services mutates `missions.state` directly — the Design Engine calls `transitionMissionState()` at the points that matter (§3), the same pattern `analysis-service.ts` established in Sprint 3.

---

## 3. Mission Engine integration

**The state-machine gap this stage must resolve, or explicitly not resolve, before code is written.** `lib/workflow/mission-state.ts`'s primary sequence is `discovered → analyzing → researching → designing → qa → proposal → email → approval → sent → archived`. Sprint 3 built `analyzing`; nothing has ever built `researching` — no Research Agent exists (`docs/11-Product-Roadmap.md`'s Sprint 3 prediction named a Research Engine and it was never built; the Founder Directive redirected Sprint 3 to Analysis instead). `transitionMissionState()` only allows the state machine's own default "next state" or one of two named exceptions (the `qa → designing` revise loop, or rejection) by default — moving `analyzing → designing` directly would require the explicit `{ allowNonSequential: true }` opt-in the function reserves for deliberate overrides, not silent convenience. This is Open Question 1, and it is the single most consequential open question in this document: Sprint 4 cannot start moving missions into `designing` without either a real (even minimal) Research stage, or a founder-approved decision to intentionally use the non-sequential override and treat `researching` as legitimately skippable for now. Guessing at this rather than deciding it explicitly would be exactly the "redesign the platform mid-implementation" failure mode `CLAUDE.md` warns against.

**Events.** Following Sprint 3's precedent (ADR-006, extended by Sprint 3's `AnalysisFailed` addition): the Design Engine needs at least one new event type recording that design generation completed (carrying, at minimum, which direction/brief was used and a reference to the generated output) and one for failure, both requiring a `lib/events/types.ts` catalog addition and a matching migration updating `mission_events.event_type`'s CHECK constraint — the same two-places-in-sync discipline every prior event addition has followed.

**Execution model.** Design generation is a multi-pass pipeline, plausibly slower and more resource-intensive than Sprint 3's seven-adapter analysis run (an LLM call per pass, potentially a render-and-screenshot step per QA iteration). ADR-012's fire-and-forget worker is the only precedent this codebase has for "don't block a request on slow multi-step work," but ADR-012 was explicit that it is a narrow workaround for one caller, not a general solution — and Sprint 4's pipeline is a heavier, more expensive version of exactly the load ADR-012 was never meant to generalize to. This document does not decide whether Sprint 4 reuses the same fire-and-forget pattern or is the sprint that finally requires the general-purpose job runner `docs/MISSION_ENGINE.md` §6 has flagged as unbuilt since Sprint 2 — that's Open Question 2, and it should be decided deliberately, the same way ADR-012 itself was a deliberate, reasoned choice rather than a default.

**Approval gate.** Per ADR-000's non-negotiable commitment (the system prepares, it never unilaterally acts), generated design output is not customer-facing until a human reviews it — this stage produces something for the `qa`/founder-review gate already in the state sequence, not something that advances autonomously past it. This review does not propose changing that.

---

## 4. Design pipeline — discussion structure, not a commitment

The founder's directive offered this conceptual pipeline as a discussion point, explicitly not a commitment: **Business Analysis → Reference Selection → Design Direction → Wireframe → Component Assembly → Typography Pass → Spacing Pass → Motion Pass → Mobile Optimization → Quality Assurance → Founder Approval → Deployment.** Reacting to it stage by stage, against what Sprint 3 already proved works and what §1's research surfaced:

- **Business Analysis** — not a new step to build; this is Sprint 3's output, already real. The Design Brief service (§2) should read `website_analyses` and the assembled `OpportunityReport` directly rather than re-deriving business context from scratch. This is the strongest, most concrete argument for sequencing the Design stage after Analysis (which Sprint 3 already did) — the redesign should be provably grounded in the *actual* measured weaknesses of the business's current site, not a generic "modern redesign," the same evidence-first standard ADR-013 set for the report itself.
- **Reference Selection** — real, but scoped narrowly per §5: informs the Design Brief's direction reasoning, cited not copied. Whether this draws from a licensed third-party service or an in-house curated set is Open Question 4.
- **Design Direction** — the text-first brief (§2's `design-brief-service.ts`). This is the highest-leverage review point in the whole pipeline: cheap to read, cheap to redirect, and it's the artifact a founder should plausibly be able to approve or redirect *before* any generation cost is spent, mirroring how `docs/SPRINT_3_DESIGN_REVIEW.md` itself was reviewed and approved before Sprint 3's implementation began.
- **Wireframe → Component Assembly → Typography Pass → Spacing Pass → Motion Pass** — plausible as the internal structure of `design-generation-service.ts`'s multiple passes (§2), each independently gradable rather than one generation call doing everything — the direct application of §1's "generic is a workflow problem" finding. Whether these five are separate service calls, separate LLM calls within one service, or some other decomposition is an implementation decision, correctly left for the next phase rather than pre-decided here.
- **Mobile Optimization** — should not be a bolt-on last pass; if it's genuinely last in sequence, it risks being the pass most likely to get compressed under time pressure. Worth an explicit decision on whether mobile is a separate late pass (as listed) or a constraint threaded through every earlier pass (closer to how modern responsive design actually works) — Open Question 5.
- **Quality Assurance** — this is `design-qa-service.ts` (§2), and it is where §1's graded, capped, screenshot-based refinement loop belongs. This is also the natural place to enforce §6's acceptance criteria mechanically, the same way `opportunity-report-service.test.ts`'s banned-terms scan mechanically enforces "no jargon" rather than relying on a human to notice a violation.
- **Founder Approval** — maps directly onto the existing `qa`/approval states already in the mission sequence; not a new concept, just this stage's instance of it.
- **Deployment** — explicitly out of scope for this review. `lib/workflow/mission-state.ts` deliberately does not carry Sprint 1's standalone `deployment` state forward (folded into `qa`, per ADR-005's migration mapping) — whatever "deployment" means for a generated client website (a shareable preview link? an actual hosting target?) is a genuinely new problem this codebase hasn't touched, named here as Open Question 6 rather than answered.

**This review's judgment on the pipeline as a whole:** the stage-by-stage structure is sound and consistent with how Sprint 3 was actually built — it should be adapted, not adopted verbatim, with the changes above (Business Analysis is already-done input, not a new step; Reference Selection is direction-only per §5; Mobile Optimization's placement needs an explicit decision rather than defaulting to "last").

---

## 5. AI responsibilities — and the hard line on bespoke output

What the Design Engine's AI components should and should not do, stated as plainly as `docs/CLAUDE.md`'s existing rules do for the report:

- **Ground every design decision in the mission's actual analysis data.** The Design Brief must cite specific Insights or Normalized Analysis findings it's responding to — the visual-domain equivalent of ADR-013's "every report claim traces to a specific measurement." A brief that can't point to what it's addressing shouldn't generate anything.
- **Reference libraries inform taste, never supply structure.** This is the line `docs/VISION_GUARDRAILS.md`'s "not a template marketplace" guardrail draws, made concrete for this pipeline: a reference may be cited in the Design Brief as "informed by [industry] sites emphasizing X" — it must never be the source of the actual section order, copy structure, or component tree that ships. If this line is hard to enforce mechanically (unlike the banned-terms test, "did you copy a template" is not cleanly checkable by a string scan), that difficulty should be treated as a real, named risk (§7), not quietly assumed away because it's inconvenient to verify.
- **Never fabricate business content.** Generated copy, claims, and positioning must be grounded in real company/mission data (`companies` table fields, the business's own actual website content captured during Analysis, real Insights) — never invented biographical or business detail presented as fact. This extends ADR-013's "no unsupported claims" standard from report language into generated site copy, where the stakes of a fabricated claim are arguably higher (it would appear on a live, published site, not an internal report).
- **Self-QA before presenting to a founder.** The QA pass (§2, §4) is not optional polish — a design that hasn't passed its own graded criteria shouldn't reach founder review, the same way an Opportunity Report category that failed its check shows "Unavailable" rather than a fabricated-looking result.
- **Motion and visual restraint follow the discipline `docs/09-UI-Design-System.md` established, adapted per business, not copied verbatim.** A generated site's typography and color palette should be industry-appropriate to the client, not Obsidian's own navy/dark tokens (§1) — but the *discipline* (real hierarchy over decoration, 200–300ms-class restrained transitions rather than bounce/spring, no gratuitous animation) should be the default baseline every generated site starts from, deviated from only when a specific business's positioning genuinely calls for something bolder.

---

## 6. Acceptance criteria (proposed, for the review's discussion — not yet approved as final)

Engineering acceptance, mirroring the concrete, checkable standard Sprint 3 set for itself:

1. A generated site's Design Brief cites at least one specific Insight or Normalized Analysis finding it addresses — mechanically checkable (the brief has a non-empty, structured citation field), not just narratively true.
2. The QA pass grades every one of its named dimensions and blocks founder-review handoff on any CRITICAL grade, capped at a fixed, documented number of refinement iterations (not an unbounded loop).
3. No two generated sites for different industries in the same review batch share identical section structure/copy skeleton — the most direct mechanical proxy available for "not a template," acknowledging (per §5) that this is a proxy, not a complete guarantee.
4. Mobile rendering is verified, not assumed — whatever the final pipeline decision on Open Question 5, a mobile-viewport render must be part of the QA pass's actual evidence, the same way Sprint 3 required real end-to-end validation against a live site rather than trusting the code.
5. Generated copy passes the same category of banned/unsupported-claims check `opportunity-report-service.test.ts` already established for report text — extended to cover generated site copy.

**Qualitative bar**, stated the way `docs/SPRINT_3_DESIGN_REVIEW.md`'s own Success Criteria section separated qualitative from engineering acceptance: a founder looking at a generated site's output should be able to tell, without being told, which real weakness in the business's current site it's addressing — and should not be able to guess, from the output alone, that it came from a shared underlying system rather than being designed for that specific business.

---

## 7. Risks

**Reference-library-as-template-in-disguise is the highest-priority risk in this document.** It's the one place this pipeline could quietly violate `docs/VISION_GUARDRAILS.md`'s explicit "not a template marketplace" guardrail without anyone deciding to — not through a deliberate choice, but through the ordinary drift of an AI generation pass leaning on cited references more heavily than intended because it's the easiest path to a plausible-looking result, the same failure mode §1's research names as "generic is a workflow problem." §5's citation requirement is a mitigation, not a guarantee — this needs active review during implementation, not a one-time design-doc mention.

**Cost and latency compound Sprint 3's known, unresolved fire-and-forget-worker gap.** A multi-pass generation-plus-QA pipeline, each pass potentially its own LLM call, each QA iteration potentially a render-and-screenshot step, is meaningfully more expensive and slower than Sprint 3's seven-adapter analysis run — and Sprint 3's execution model was already flagged (ADR-012) as unverified against real serverless function-freezing behavior. This risk compounds rather than introduces a new one, but it compounds enough that Open Question 2 should be resolved deliberately, not by default inheritance of Sprint 3's pattern.

**The `researching` state gap is a real sequencing risk, not just a documentation nuance.** If Sprint 4 ships a Design Engine that calls `transitionMissionState()` with `allowNonSequential: true` to skip `researching` without an explicit founder decision recorded (the way ADR-012 recorded the async-execution tradeoff), that's exactly the kind of "redesign mid-implementation without flagging it" `CLAUDE.md` warns against, even though the mechanism (the override flag) already exists and would technically work.

**Mechanically checking "not generic" is harder than mechanically checking "no adapter names in this string."** Sprint 3's banned-terms test worked because "does this string contain the word Lighthouse" is a clean, deterministic check. "Does this design look like a template" has no equivalent clean check — §6's proposed proxy (structural diversity across a batch) is real but partial, and this gap should be named honestly rather than implied to be as solved as Sprint 3's jargon check was.

**Third-party design-reference dependency risk** — cost, licensing, and availability of any external reference-library service, if Open Question 4 resolves toward using one, is an ordinary vendor-dependency risk this codebase hasn't had to reason about yet (Sprint 1–3's external dependencies are all developer tooling — Lighthouse, axe-core, Puppeteer — not a paid content/data service).

---

## 8. Open questions

1. **Does Sprint 4 need a minimal Research stage, or does it deliberately skip `researching` via the non-sequential override?** The single most consequential open question in this document (§3) — needs an explicit founder decision, not an implementation-time default.
2. **Where does the multi-pass generation pipeline actually run?** Reuse Sprint 3's fire-and-forget pattern (ADR-012) as-is, extend it, or is this the sprint that finally builds the general-purpose job runner `docs/MISSION_ENGINE.md` §6 has named unbuilt since Sprint 2? (§3)
3. **Should any generation mode intentionally produce bolder, less restrained layouts for businesses whose industry/positioning calls for it**, and if so, is that a per-mission direction decision made in the Design Brief, or a distinct, explicitly-scoped Phase 2 capability (the way Sprint 3's design doc scoped six qualitative signals out to a named future phase rather than building them halfway)? (§1)
4. **Third-party design-reference platform vs. in-house-curated reference set** — cost, licensing, and whether a smaller owned set is sufficient before committing to an external dependency. (§1, §7)
5. **Is Mobile Optimization a distinct late pass, or a constraint threaded through every earlier pass?** (§4)
6. **What does "Deployment" mean for a generated client website** — a shareable preview link, an actual hosting target, something else? Explicitly out of scope for this review, named so it isn't silently assumed away. (§4)
7. **Should the Design stage read the Opportunity Score at all, given `docs/SPRINT_3_REVIEW.md` flagged its category weighting as an unresolved founder decision?** Reading Insights and Normalized Analysis directly (not the aggregate score) may be sufficient for the Design Brief and would sidestep depending on a number nobody has signed off on yet — worth deciding explicitly rather than defaulting to "use whatever data is available."

---

## 9. What this review is asking the founder to decide before implementation starts

Everything in §8, but concretely: this document is not requesting sign-off to begin building. It's requesting a decision on Open Questions 1 and 2 in particular (the state-machine sequencing gap and the execution-model question), since both would materially change the shape of the first code written, and getting either wrong would mean redesigning mid-implementation — exactly the failure mode `CLAUDE.md` asks to be avoided by deciding up front instead.
