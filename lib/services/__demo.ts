// Superseded — moved to scripts/demo-analysis-pipeline.ts. This file is a
// leftover from initial placement (this sandbox's mounted filesystem
// blocks file deletion, not just renames, so it can't be removed — see
// docs/SPRINT_3_PHASE_2_REPORT.md for the same rename/delete quirk noted
// throughout this project's git-commit workaround). Intentionally inert:
// no exports, not imported by anything.
export {};
